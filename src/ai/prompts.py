"""
AI 提示词模板

定义了与 LLM 交互的各种提示词，包括：
1. 步骤解析提示词
2. Playwright 操作生成提示词
3. 执行结果分析提示词
4. 报告总结提示词
"""

# ============================================================================
# 步骤解析提示词：将自然语言测试步骤转换为结构化步骤
# ============================================================================
STEP_PARSE_SYSTEM = """你是一个专业的测试工程师。你的任务是将自然语言描述的测试用例步骤，
拆解为独立的结构化操作步骤。

请仔细分析操作描述中的编号（如"1、""2、"）和多行格式，
将其拆分为独立的步骤。每个步骤需要包含：
- index: 步骤编号
- action: 操作类型 (navigate/click/input/wait/assert/scroll/observe)
- description: 该步骤的具体描述
- target: 操作目标元素（按钮名、菜单名、输入框名等）
- expected: 该步骤的预期结果（如果有）

输出格式：返回 JSON 数组。"""

STEP_PARSE_USER = """请分析以下测试用例，将其操作描述拆解为独立步骤：

用例名称：{case_name}
步骤类型：{step_type}
前置条件：{precondition}
操作描述：{operation_desc}
预期结果：{expected_result}

请输出 JSON 格式的步骤列表：
```json
[
  {{
    "index": 1,
    "action": "click",
    "description": "点击某某菜单",
    "target": "菜单名称",
    "expected": "菜单展开"
  }}
]
```"""

# ============================================================================
# Playwright 操作生成提示词：将步骤转化为可执行的 Playwright 代码
# ============================================================================
ACTION_GEN_SYSTEM = """你是一个 Playwright 自动化测试专家。你的任务是将测试步骤转换为可执行的 Playwright 操作指令。

应用程序：同花顺远航版（THS Ocean 客户端）——一个 Windows 股票交易客户端。
应用程序窗口标题包含"同花顺"关键字。

可用操作类型及参数说明：

1. click - 点击元素
   target: 元素的文本或角色描述，如 "通用工具"、"示例板块"
   strategy: "text"(匹配文本) | "role"(匹配语义角色) | "selector"(CSS/XPath)
2. dblclick - 双击元素
3. wait - 等待
   value: 等待时间(毫秒)
4. input - 输入文本
   target: 输入框描述
   value: 要输入的文本
5. screenshot - 截图
   value: 截图文件名（可选）
6. assert - 断言
   target: 要检查的内容描述
   value: 可选的具体匹配值
7. observe - 观察（截图后由 AI 判断）
8. scroll - 滚动
   direction: "up"|"down"|"left"|"right"
9. hover - 悬停
   target: 悬停目标

注意：
- 如果操作描述中提到"观察"，优先使用 screenshot + observe 而非简单 click。
- 对于菜单层级导航，按层级逐级点击：click("一级菜单") -> wait -> click("二级菜单")。
- 如果描述中包含"拖动窗口边缘"，使用 resize 操作。
- 如果描述中包含数字（如"1、""2、"），应拆分为多个独立操作。

输出纯 JSON 数组，不要有任何额外文本。"""

ACTION_GEN_USER = """请将以下测试步骤转换为 Playwright 操作指令列表：

用例名称：{case_name}
前置条件：{precondition}
步骤描述：
{steps_text}

预期结果：{expected_result}

上下文：
- 当前已在同花顺远航版主窗口
- 客户端已登录

请生成 JSON 操作指令数组。"""

# ============================================================================
# 执行结果分析提示词：AI 分析截图和执行日志，判断测试是否通过
# ============================================================================
RESULT_ANALYSIS_SYSTEM = """你是一个专业的测试结果分析员。你的任务是根据测试步骤、执行日志和截图，
判断测试用例是否通过，并分析未通过原因。

分析维度：
1. 可执行性：测试步骤是否能被完整执行？是否遇到找不到元素的错误？
2. 一致性：实际执行结果是否符合预期结果描述？
3. 完整性：步骤描述是否足够清晰完整？
4. 正确性：预期结果是否与当前软件版本一致？

请给出专业判断，并附加清洗标签和建议。"""

RESULT_ANALYSIS_USER = """请分析以下测试用例的执行结果：

用例名称：{case_name}
预期结果：{expected_result}
执行日志：{execution_log}

请输出 JSON 格式的分析结果：
```json
{{
  "passed": true/false,
  "confidence": 0.0-1.0,
  "reason": "判断理由",
  "tags": ["标签1", "标签2"],
  "suggestion": "修复建议（如果不通过）",
  "analysis_detail": "详细分析"
}}
```

可能的标签：
- "通过"：测试完全通过
- "未通过"：测试执行未通过
- "组件缺失"：测试涉及的UI组件在当前版本中不存在
- "预期不一致"：预期结果与实际表现不符
- "步骤不足"：操作步骤描述不够详细
- "描述不清"：步骤描述模糊
- "可执行"：用例可以执行
- "不可执行"：用例无法执行"""

# ============================================================================
# 报告总结提示词：生成整体清洗报告摘要
# ============================================================================
REPORT_SUMMARY_SYSTEM = """你是一个测试质量分析师。请根据清洗统计数据，生成一份专业的清洗报告摘要。
摘要应该包括：
1. 总体情况概述
2. 问题分类统计
3. 关键发现
4. 改进建议
5. 下一步行动建议

语言：中文
风格：专业、简洁、有数据支撑"""

REPORT_SUMMARY_USER = """请生成测试用例清洗报告摘要：

总体统计：
- 总用例数：{total}
- 通过数：{passed}
- 未通过数：{failed}
- 不可执行数：{not_executable}

标签分布：
{tag_stats}

失败用例详情：
{failure_details}

应用名称：同花顺远航版
测试模块：{module_name}
"""
